import ImageSlider from "../../components/storefront/home/image-slider";

const Home = () => {
  return (
    <main className="flex items-center justify-center py-10">
      <ImageSlider />
    </main>
  );
};

export default Home;
